# Repo-root ignores only.
# Each surface keeps its own .gitignore for build artifacts (e.g. app/.gitignore
# ignores Flutter's build/ and .dart_tool/).

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
